---

- name: Setup Apache Server
  hosts: localhost

  tasks:
    - name: Install Apache server
      apt:
        name: apache2
        state: present
      tags: i-apache2

    - name: Start Apache server
      service:
        name: apache2
        state: started
      tags: s-apache2  
